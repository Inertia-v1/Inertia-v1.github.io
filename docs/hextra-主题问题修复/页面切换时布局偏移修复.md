# 页面切换时布局偏移修复

## 现象

在文档页间切换时，内容区域整体左移/右移跳动。

## 原因

内容短的页面无滚动条，内容长的页面出现滚动条（约 15px），导致 `clientWidth` 变化，页面布局偏移。

## 修复

在 `assets/css/custom.css` 中添加：

```css
html {
  overflow-y: scroll;
}
```

滚动条始终占位，页面宽度恒定。

